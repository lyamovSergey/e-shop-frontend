import { SalerLayout } from '@/components/layouts/saler-layout/SalerLayout'

export default function Layout({ children }: { children: React.ReactNode }) {
	return <SalerLayout>{children}</SalerLayout>
}
